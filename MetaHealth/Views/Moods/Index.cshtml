@model IEnumerable<MetaHealth.Mood>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.MoodsInBetween.FK_UserTable)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.MoodNum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MoodsInBetween.AspNetUser.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MoodNum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { id=item.PK })
        </td>
    </tr>
}

</table>
